{ delib, inputs, ... }:
let
  _delib :: ProjectDelib; _delib = delib;
  _inputs :: Inputs; _inputs = inputs;
in
_delib.overlayModule {
  name = "nix-vscode-extensions";
  overlay = _inputs.nix-vscode-extensions.overlays.default;
  targets = [ "nixos" "darwin" ];
  enabled = true;
}
